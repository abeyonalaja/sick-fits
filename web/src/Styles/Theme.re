open Css;
let red = `hex("ff0000");
let black = `hex("393939");
let lightgrey = `hex("E1E1E1");
let offWhite = `hex("EDEDED");
let maxWidth = 1000;
let bshadow = boxShadow(~x=px(0), ~y=px(12), ~blur=px(24), ~spread=px(0), `rgba((0, 0, 0, 0.09)));
